#include <stdio.h>
#include <conio.h>

int main()
{
    int a, b, sum;
    printf("Input A and B: ");
    scanf("%d%d", &a, &b);
    sum = a + b;
    printf("Result %d + %d = %d\n", a, b, sum);
    return 0;
}
